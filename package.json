{
  "name": "astro-handbook",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && npx gh-pages -d dist"    
  },
  "dependencies": {
    "@astrojs/mdx": "^4.3.0",
    "astro": "^5.10.1"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
